{% extends "base.html.twig" %}
{% block title %}
Quack - Messages
{% endblock %}
{% block body %}

  </tbody> 
    </div>
    <table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Message</th>
      <th scope="col">Modifier</th>
      <th scope="col">Supprimer</th>
    </tr>
  </thead>
  <tbody>
  {% for message in messages  %}
    <tr class="table-light">
      <td>{{message.message}}</td>
      <td><a href="{{ path('message.edit', {id : message.id})}}" class=" btn btn-info"> Modifier</a></td>
      <td><a href="{{ path('message.delete', {id : message.id})}}" class=" btn btn-warning"> Supprimer</a></td>
    </tr>
      {% endfor %}
  </tbody>
</table>
{% endblock %}